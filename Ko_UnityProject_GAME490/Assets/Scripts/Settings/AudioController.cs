using System.IO;
using System.Collections;
using System.Collections.Generic;
using UnityEngine;

namespace KATTH
{
    public class AudioController : MonoBehaviour
    {
        /*[SerializeField]
        private Profiles m_profiles;

        [SerializeField]
        private List<KatthSliders> m_VolumeSliders = new List<KatthSliders>();

        // Start is called before the first frame update
        //void Start()
        {
            if (ProfilesManager.profile && ProfilesManager.profile.audioMixer != null)
                ProfilesManager.profile.SaveAudioLevels();
        }

        public void ApplyChanges()
        {
            if (ProfilesManager.profile && ProfilesManager.profile.audioMixer != null)
                ProfilesManager.profile.SaveAudioLevels();
        }

        public void CancelChanges()
        {
            if (ProfilesManager.profile && ProfilesManager.profile.audioMixer != null)
                ProfilesManager.profile.SaveAudioLevels();

            for(int i = 0; i < m_VolumeSliders.Count; i++)
            {
                m_VolumeSliders[i].ResetSliderValue();
            }
        }*/

        
    }
}
